import 'package:flutter/material.dart';

class TvsPage extends StatelessWidget {
  const TvsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('TVs Page')),
    );
  }
}
